import React from "react";
import { reviewType } from "@/types/productType";

type Props = {
  reviewList: reviewType[];
};

const PhotoReviewContainer = ({ reviewList }: Props) => {
  return <div>reviewList</div>;
};

export default PhotoReviewContainer;
